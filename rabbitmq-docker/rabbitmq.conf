
management.ssl.port       = 15671
management.ssl.cacertfile = /tmp/domain.crt
management.ssl.certfile   = /tmp/domain.crt
management.ssl.keyfile    = /tmp/domain.key



    # listeners.tcp = none
    # listeners.ssl.default = 5671
    # ssl_options.cacertfile = / tmp / rootCA.crt
    # ssl_options.certfile = / tmp / mydomain.crt
    # ssl_options.keyfile = / tmp / mydomain.key
    # ssl_options.verify = verify_peer
    # ssl_options.fail_if_no_peer_cert = true
